@model IEnumerable<RelogioPonto.Models.Relogio>

@{
    ViewBag.Title = "Index";
}
<br>
<h2>QUANTIDADE PAPEL RELÓGIO DE PONTO</h2>
<br>

<h2 align="center">
    @Html.ActionLink("CRIAR NOVO STATUS DE RELÓGIO", "Create")
</h2>



@foreach (var item in Model)
{

    <div class="row">

        <div class="col-xs-12  caixa ">
            <h3>Nome: @item.Nome</h3>
            <hr />
            <h3>Descrição: @item.Descricao</h3>
            <hr />
            <h3>IP: @item.Ip</h3>
            <hr />
            <h3>Status: @item.Status </h3>
        </div>

        @*<div class="col-md-4 ">
            <div>
                @item.Nome
            </div>

            <div>
                @item.Descricao
            </div>

            <div>
                @item.Ip
            </div>

        </div>*@

    </div>
    
        <tr>
            <td>

                @*@Html.ActionLink("Editar", "Edit", new { id = item.Id }) |*@
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }) |
                @Html.ActionLink("Deletar", "Delete", new { id = item.Id })
            </td>
        </tr>
    
}

